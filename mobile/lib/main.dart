import 'package:flutter/material.dart';

import 'week/week_app.dart';

/// Entry point for the backend-free week viewer.
/// Build with: flutter build web --release --pwa-strategy=none
void main() => runApp(const WeekApp());
